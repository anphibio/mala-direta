services:
  mala-direta:
    build:
      context: .
    container_name: mala-direta-tceal
    ports:
      - "8086:8086"
    environment:
      APP_HOST: 0.0.0.0
      APP_PORT: 8086
    restart: unless-stopped
